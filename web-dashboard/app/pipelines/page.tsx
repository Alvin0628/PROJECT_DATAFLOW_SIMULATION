import AutoRefresh from "@/components/AutoRefresh";
import { getBaseUrl } from "@/lib/get-base-url";
interface AirflowDag { dag_id: string; is_active: boolean; is_paused: boolean; description: string | null; owners: string[]; next_dagrun: string | null; }
export default async function PipelinesPage() { const res = await fetch(`${getBaseUrl()}/api/airflow/dags`, { cache: "no-store" }); const body = await res.json(); if (body.error) return <div className="dashboard-panel border-danger/30 bg-danger-soft p-5 text-sm text-danger">Unable to reach Airflow: {body.error}</div>; const dags: AirflowDag[] = body.data ?? []; const active = dags.filter((dag) => !dag.is_paused).length; return <div className="flex flex-col gap-8 pb-10"><AutoRefresh intervalMs={60000} /><header className="border-b border-border pb-7"><p className="dashboard-eyebrow">Runtime / orchestration</p><h1 className="mt-2 text-4xl font-semibold tracking-[-0.04em]">Pipeline control room</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Observe workflow health, execution posture, and scheduling context across the Airflow runtime.</p></header><section className="grid gap-4 md:grid-cols-4"><div className="dashboard-panel bg-primary/10 p-5"><p className="dashboard-eyebrow">Workflow health</p><p className="mt-3 text-3xl font-semibold text-success">Operational</p></div><div className="dashboard-panel p-5"><p className="dashboard-eyebrow">Active DAGs</p><p className="mt-3 text-3xl font-semibold">{active}</p><p className="text-sm text-muted">of {dags.length} registered</p></div><div className="dashboard-panel p-5"><p className="dashboard-eyebrow">Paused</p><p className="mt-3 text-3xl font-semibold">{dags.length - active}</p><p className="text-sm text-muted">manual intervention</p></div><div className="dashboard-panel p-5"><p className="dashboard-eyebrow">Scheduler</p><p className="mt-3 text-3xl font-semibold">LIVE</p><p className="text-sm text-muted">Airflow REST API</p></div></section><section className="grid gap-6 xl:grid-cols-[1.25fr_0.75fr]"><div className="dashboard-panel p-5 md:p-6"><div className="flex items-center justify-between"><div><p className="dashboard-eyebrow">Execution topology</p><h2 className="mt-1 text-xl font-semibold">DAG runtime lanes</h2></div><span className="rounded-full bg-success-soft px-3 py-1 font-mono text-[10px] text-success">{active} RUNNING</span></div><div className="mt-8 flex flex-col gap-3">{["Ingest", "Transform", "Train", "Infer", "Serve"].map((step, index) => <div key={step} className="flex items-center gap-4"><div className="flex size-9 shrink-0 items-center justify-center rounded-lg border border-primary/40 bg-primary/10 font-mono text-xs text-primary">0{index + 1}</div><div className="h-px flex-1 bg-border"/><div className="w-28 font-mono text-xs text-muted">{step}</div><span className="size-2 rounded-full bg-success"/></div>)}</div></div><div className="dashboard-panel p-5 md:p-6"><p className="dashboard-eyebrow">Scheduler context</p><h2 className="mt-1 text-xl font-semibold">Next runs</h2><div className="mt-6 flex flex-col gap-4">{dags.slice(0, 4).map((dag) => <div key={dag.dag_id} className="border-b border-border pb-4 last:border-0"><p className="truncate font-mono text-xs font-semibold">{dag.dag_id}</p><p className="mt-1 text-xs text-muted">{dag.next_dagrun ? new Date(dag.next_dagrun).toLocaleString("id-ID") : "Not scheduled"}</p></div>)}</div></div></section><section className="dashboard-panel overflow-hidden"><div className="border-b border-border px-5 py-4 md:px-6"><p className="dashboard-eyebrow">Airflow registry</p><h2 className="mt-1 text-xl font-semibold">Runtime state</h2></div><div className="overflow-x-auto"><table className="dashboard-table w-full min-w-[720px] text-left text-sm"><thead><tr><th className="px-5 py-3">DAG ID</th><th className="px-5 py-3">State</th><th className="px-5 py-3">Owner</th><th className="px-5 py-3">Next run</th></tr></thead><tbody>{dags.length === 0 ? <tr><td colSpan={4} className="px-5 py-10 text-center text-muted">No DAGs found.</td></tr> : dags.map((dag) => <tr key={dag.dag_id}><td className="px-5 py-4 font-mono text-xs font-semibold">{dag.dag_id}</td><td className="px-5 py-4"><span className={dag.is_paused ? "rounded-full bg-surface-muted px-2 py-1 font-mono text-[10px] text-muted" : "rounded-full bg-success-soft px-2 py-1 font-mono text-[10px] text-success"}>{dag.is_paused ? "PAUSED" : "ACTIVE"}</span></td><td className="px-5 py-4 text-muted">{dag.owners.join(", ")}</td><td className="px-5 py-4 font-mono text-xs text-muted">{dag.next_dagrun ? new Date(dag.next_dagrun).toLocaleString("id-ID") : "Not scheduled"}</td></tr>)}</tbody></table></div></section></div>; }
