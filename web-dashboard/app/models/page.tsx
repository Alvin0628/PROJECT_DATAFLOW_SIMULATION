import AutoRefresh from "@/components/AutoRefresh";
import { Suspense } from "react";
import ModelHistoryTable from "@/components/models/ModelHistoryTable";
import Link from "next/link";

const models = [{ name: "Session Conversion", key: "session_conversion", detail: "Predictive signal quality and champion history." }, { name: "Customer Churn", key: "customer_churn", detail: "Quality-gated challenger evaluation history." }] as const;

export default function ModelsPage() {
  return <div className="flex flex-col gap-8 pb-10"><AutoRefresh intervalMs={60000} /><header className="flex flex-col gap-5 border-b border-border pb-7 lg:flex-row lg:items-end lg:justify-between"><div><p className="dashboard-eyebrow">Registry / lifecycle control</p><h1 className="mt-2 text-4xl font-semibold tracking-[-0.04em]">Model operations</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">A working view of production candidates, champion lineage, and the training runs behind every decision.</p></div><div className="flex gap-2 font-mono text-[10px] uppercase tracking-widest text-muted"><span className="rounded-full border border-success/30 bg-success-soft px-3 py-2 text-success">2 registered</span><span className="rounded-full border border-border bg-surface-muted px-3 py-2">Quality gate active</span></div></header><section className="grid gap-4 md:grid-cols-3"><article className="dashboard-panel bg-gradient-to-br from-primary/15 to-surface p-5"><p className="dashboard-eyebrow">Champion</p><h2 className="mt-3 text-xl font-semibold">customer_churn</h2><p className="mt-1 text-sm text-muted">Serving production traffic</p><div className="mt-6 flex items-end justify-between"><strong className="text-3xl">v2</strong><span className="rounded-full bg-success-soft px-2 py-1 font-mono text-[10px] text-success">HEALTHY</span></div></article><article className="dashboard-panel p-5"><p className="dashboard-eyebrow">Registry coverage</p><p className="mt-3 text-3xl font-semibold">2</p><p className="mt-1 text-sm text-muted">Models with tracked history</p><div className="mt-5 h-2 overflow-hidden rounded-full bg-surface-muted"><div className="h-full w-full rounded-full bg-primary" /></div></article><article className="dashboard-panel p-5"><p className="dashboard-eyebrow">Review queue</p><p className="mt-3 text-3xl font-semibold">1</p><p className="mt-1 text-sm text-muted">Challenger awaiting evaluation</p><Link href="/predictions" className="mt-5 inline-flex text-sm font-semibold text-primary">Inspect live output →</Link></article></section><section className="dashboard-panel overflow-hidden"><div className="flex items-center justify-between border-b border-border px-5 py-4 md:px-6"><div><p className="dashboard-eyebrow">Model registry</p><h2 className="mt-1 text-lg font-semibold">Lifecycle and training history</h2></div><span className="font-mono text-[10px] uppercase tracking-widest text-muted">Latest first</span></div><div className="flex flex-col gap-8 p-5 md:p-6">{models.map((model, index) => <section key={model.key} className="grid gap-6 xl:grid-cols-[0.8fr_2fr]"><div className="flex flex-col justify-between gap-5"><div><div className="flex items-center gap-3"><span className="font-mono text-xs text-primary">0{index + 1}</span><h3 className="text-xl font-semibold">{model.name}</h3></div><p className="mt-2 text-sm leading-6 text-muted">{model.detail}</p></div><div className="grid grid-cols-2 gap-3 text-sm"><div className="rounded-lg border border-border bg-surface-muted p-3"><p className="dashboard-eyebrow">State</p><p className="mt-2 font-semibold">{index === 0 ? "Champion" : "Challenger"}</p></div><div className="rounded-lg border border-border bg-surface-muted p-3"><p className="dashboard-eyebrow">Runs</p><p className="mt-2 font-semibold">Tracked</p></div></div></div><Suspense fallback={<div className="rounded-lg border border-dashed border-border p-8 text-center font-mono text-xs text-muted">LOADING TRAINING HISTORY...</div>}><ModelHistoryTable modelName={model.key} /></Suspense></section>)}</div></section></div>;
}
